services:
  # 1. Database 설정
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_USER=myuser'
      - 'POSTGRES_PASSWORD=secret'
    ports:
      - '5432:5432'

  # 2. Redis 설정
  redis:
    image: 'redis:latest'
    ports:
      - '6379:6379'


  api-server:
    image: 'wale12/git-action-test:latest' # 빌드 대신 이미지를 가져옵니다.
    ports:
      - '80:8080'
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/mydatabase
      - SPRING_DATASOURCE_USERNAME=myuser
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_DATA_REDIS_HOST=redis
      - SPRING_DATA_REDIS_PORT=6379
    depends_on:
      - postgres
      - redis

  # 3. Spring Boot 서버 설정 (내 프로젝트 빌드)
#  api-server:
#    build: .  # 현재 폴더의 Dockerfile을 사용해 이미지를 만듦
#    ports:
#      - '8080:8080'
#    environment:
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/mydatabase
#      - SPRING_DATASOURCE_USERNAME=myuser
#      - SPRING_DATASOURCE_PASSWORD=secret
#      - SPRING_DATA_REDIS_HOST=redis
#      - SPRING_DATA_REDIS_PORT=6379
#    depends_on:
#      - postgres
#      - redis